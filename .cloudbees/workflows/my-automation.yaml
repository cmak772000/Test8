apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: Say hello1
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo ${{ vars.string-property }} ${{ secrets.string-secret }} test1
